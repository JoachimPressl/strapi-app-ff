{
  "collectionName": "components_layout_news_lists",
  "info": {
    "displayName": "newsBlock",
    "icon": "bulletList"
  },
  "options": {},
  "attributes": {
    "mode": {
      "type": "enumeration",
      "required": true,
      "default": "limit",
      "enum": [
        "limit",
        "pages"
      ]
    },
    "pages_or_limit": {
      "type": "integer",
      "required": true,
      "default": 6
    }
  },
  "config": {}
}
