{
  "collectionName": "components_layout_appearances",
  "info": {
    "displayName": "frameBlock",
    "icon": "brush"
  },
  "options": {},
  "attributes": {
    "layout": {
      "type": "enumeration",
      "required": true,
      "default": "default",
      "enum": [
        "default",
        "primary",
        "secondary",
        "tertairy"
      ]
    },
    "frame": {
      "type": "enumeration",
      "required": true,
      "default": "default",
      "enum": [
        "default",
        "no_frame"
      ]
    },
    "container": {
      "type": "enumeration",
      "required": true,
      "default": "default",
      "enum": [
        "default",
        "small",
        "large"
      ]
    },
    "background": {
      "type": "enumeration",
      "required": true,
      "default": "default",
      "enum": [
        "default",
        "light",
        "dark",
        "primary",
        "secondary",
        "tertairy"
      ]
    },
    "space_before": {
      "type": "enumeration",
      "required": true,
      "default": "medium",
      "enum": [
        "default",
        "extra_small",
        "small",
        "medium",
        "large",
        "extra_large"
      ]
    },
    "space_after": {
      "type": "enumeration",
      "required": true,
      "default": "medium",
      "enum": [
        "default",
        "extra_small",
        "small",
        "medium",
        "large",
        "extra_large"
      ]
    }
  },
  "config": {}
}
