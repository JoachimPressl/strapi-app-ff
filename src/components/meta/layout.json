{
  "collectionName": "components_meta_layouts",
  "info": {
    "displayName": "layout",
    "icon": "crop"
  },
  "options": {},
  "attributes": {
    "space_before": {
      "type": "enumeration",
      "default": "default",
      "enum": [
        "default",
        "extra small",
        "small",
        "medium",
        "large",
        "extra large"
      ]
    },
    "space_after": {
      "type": "enumeration",
      "default": "default",
      "enum": [
        "default",
        "extra small",
        "small",
        "medium",
        "large",
        "extra large"
      ]
    },
    "background_color": {
      "type": "enumeration",
      "default": "default",
      "enum": [
        "default",
        "dark",
        "light",
        "primary"
      ]
    },
    "template": {
      "type": "enumeration",
      "default": "default",
      "enum": [
        "default",
        "secondary"
      ]
    }
  },
  "config": {}
}
