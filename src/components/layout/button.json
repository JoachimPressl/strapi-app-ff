{
  "collectionName": "components_layout_buttons",
  "info": {
    "displayName": "buttonBlock",
    "icon": "link"
  },
  "options": {},
  "attributes": {
    "text": {
      "type": "string"
    },
    "link": {
      "type": "string"
    },
    "target": {
      "type": "enumeration",
      "required": true,
      "default": "default",
      "enum": [
        "default",
        "new window"
      ]
    },
    "style": {
      "type": "enumeration",
      "required": true,
      "default": "default",
      "enum": [
        "default",
        "primary",
        "secondary",
        "tertairy"
      ]
    }
  },
  "config": {}
}
